// Import Style
import styled from 'styled-components';

// Import Constant
import { starWidth } from '../../constants';

export const StarSpan = styled.span`
  display: inline-block;
  width: ${starWidth * 5}px;
`;
